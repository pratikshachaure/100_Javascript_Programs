const prompt = require('prompt-sync')();  

// / 006. Write a program to print area of a circle. A(circle)= 3.142 * R * R in js

let radius=prompt("Enter your Radius :-");
let area=3.14*radius*radius;
console.log("Area of circle is :- ",area);